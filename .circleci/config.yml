version: 2.1

jobs:
  tests:
    docker:
      - image: cimg/python:3.8
    steps:
      - checkout
      - run: virtualenv ./.venv
      - run: . .venv/bin/activate
      - run: pip install -r requirements.txt 
          
          
workflows:
  build_test:
    jobs:
      - tests

